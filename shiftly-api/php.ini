; php.ini local — lu automatiquement par le Symfony CLI (symfony server:start)
; Aligné sur la prod Docker (Dockerfile : uploads.ini) pour éviter les écarts
; dev/prod sur l'upload de médias (PNG screenshots > 2 Mo notamment).
upload_max_filesize = 25M
post_max_size = 25M
memory_limit = 256M
