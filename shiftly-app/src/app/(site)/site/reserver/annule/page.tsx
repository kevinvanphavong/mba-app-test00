'use client'

import PaymentCancelled from '@/components/public/PaymentCancelled'

/** Retour de paiement annulé `/site/reserver/annule`. Résa laissée EN_ATTENTE_ACOMPTE. */
export default function AnnulePage() {
  return <PaymentCancelled />
}
